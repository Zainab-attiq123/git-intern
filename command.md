# Git Commands

1. git init  
   Initializes a new Git repository.

2. git status 
   Shows current status of files.

3. git add .
   Adds all files to staging area.

4. git commit -m "message"  
   Saves changes with a message.

5. git push
   Uploads commits to GitHub.
